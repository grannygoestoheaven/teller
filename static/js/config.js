export const elements = {}; // the elements object is defined in main.js
export let lastStoryData = {}; // the lastStoryData object is defined in storyService.js

// Global variables
export let gridSize = 5; // Default size
export let gridValues = new Map(); // { id: { value: string, size: string } }
export let isGridVisible = true; // Default visibility
